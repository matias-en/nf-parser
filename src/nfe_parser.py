# from src.processor import limpar_num, limpar_int, formatar_data

# nfe = {'nfe': ''}

# def extrair_dados(arvore):

#     def pegar_valor(xpath.str):
#         res = arvore.xpath(xpath_str, namespaces=nfe)
#         return res[0] if res else ""
    
#     def buscar_num(xpath_str):
#         return limpar_num(arvore.xpath(xpath_str, namespaces=nfe))

#     def buscar_int(xpath_str):
#         return limpar_int(arvore.xpath(xpath_str, namespaces=nfe))